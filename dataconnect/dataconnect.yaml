specVersion: "v1beta"
serviceId: "taskmaster"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "taskmaster-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
